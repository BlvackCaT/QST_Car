**工作日志--2026.8.25**

**今日主要完成了vscode-SSH和Linux虚拟机环境的搭建，了解了Windows和Linux的ip地址。研读了关于WS2812跑马灯的代码main.c和colorful_led.c，并完成对该实验的复现。**
**完成ALL_white.c和Half_red.c代码的改写，同时还对呼吸灯和其它发光灯种进行了演示**
**完成小车的ST-Link驱动调试花费较多时间，最终通过询问AI大模型（chatgpt）解决**

**学习内容：**

U8 是 unsigned char 八位无符号整数

PC13 (DIL) → 接左边 WS2812 灯带 DIN 信号脚（前灯）
PC14 (DIR) → 接右边 WS2812 灯带 DIN 信号脚（后尾灯）

DIL = 1; 等价于：让 PC13 这个 GPIO 引脚输出高电平（3.3V），反之则是低电平
先高，再变低，维持一段时间：目的是造出 WS2812 识别的一个数据比特位（0 或者 1）
波形：短高 + 长低 → 代表二进制 0
波形：长高 + 短低 → 代表二进制 1
WS2812 就是通过高电平持续时间来区分 0 和 1。

Reset:
发送颜色数据
      ↓
[数据][数据][数据]...
      ↓
保持低电平 > 一段时间
      ↓
WS2812锁存
      ↓
LED真正显示新颜色

L_ws2812_rgb(L_ws_num,ws_r,ws_g,ws_b);
L_ws_num → 第几个LED
ws_r → 红色
ws_g → 绿色
ws_b → 蓝色
WS2812 的数据发送顺序通常是 GRB，不是常见的 RGB。

L_ws_data[(Lwsnum−1)∗3]=ws_g;
L_ws_data[(Lwsnum−1)∗3+1]=ws_r;
L_ws_data[(Lwsnum−1)∗3+2]=ws_b;
每一个灯都有GRB三个数据，所以(L_ws_num-1)*3可以计算处每一个灯在数组中占的第一个位置

void L_ws2812_refresh(u8 ws_count) 
检查每一位并输出数据，GPIO输出给WS2812
一个8位数据
        ↓
逐bit拆开
        ↓
每一位变成对应的WS2812时序
        ↓
发送出去
time指当前是第几种颜色组合,根据time的值选择不同的灯光方案


**工作日志--2026.8.26**

上午主要完成了STM32的“Hello World”调试实验，使用Linux和Windows两个系统之间的共享vm_share文件夹，采用vscode远端ssh操作，输入python build.py wifiiot,得到产生的bin文件，然后使用Hiburn烧录，用Uart串口助手传输。
下午首先根据8.25的跑马灯代码对小车驱动(STM32)的代码进行了合并和改动，完成让小车启动的目标，同时Hi3816方面初步完成了对舵机的调控。

**学习内容：**

初步了解了STM32的定时器原理，PWM驱动电机的使用。
Hi3816方面学习了GPIO驱动舵机，互斥锁相关的知识还不太了解！以及Linux和Windows系统之间关于每次产生新的bin文件不能及时更新的问题，现有的解决方法是去Ubuntu手动传送，但是比较麻烦。
学习了如何使用Hiburn烧录，以及通过串口助手进行传输，并在此过程中学到了GBK和UTF-8编码的不同(传输过程中中英文输出)。

工作日志--2026.8.27
今日上午主要完成了STM32的TIMER编码器测速和PID电机速度控制，在colorful_led.c和motor.c的基础上添加了control.c和read.c,对代码进行了进一步的完善，能通过串口实时监测小车运行的速度信息，在此基础上通过PID调试小车的运动状态，包括左右轮速度，运行方向等，主要成果是完成了让小车走直线同时回退的功能。
下午完善了小车运动的功能之后，Hi3816方面进行了GPIO驱动超声波的实验，流程还是通过vs编译，得到bin文件再烧录通过串口传输，与昨天舵机类似。
